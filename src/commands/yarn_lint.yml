description: |
  This job performs linting for the project.

parameters:
  prj_path:
    type: string
    default: "./"
    description: Project path where 'package.json' lies.

steps:
  - attach_workspace:
      at: ~/project
  - run:
      name: Linting
      command: |
        cd "$prj_path"
        yarn lint
